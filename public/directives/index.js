(function() {

    define([ 
            'offer-thumbnail',
            'offer-author',
            'comments',
            'add-comment',
            'gallery',
            'image-picker',
            'login'
            ].map(function(f) { return './' + f; }),
           function() { }
    );

})();